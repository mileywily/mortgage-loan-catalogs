apiVersion: v1
kind: Service
metadata:
  name: mortgage-catalogs-go
spec:
  selector:
    app: mortgage-catalogs-go
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

